[pytest]
testpaths = tools
pythonpath = tools
norecursedirs = backend worker frontend caddy docs out .git .venv __pycache__
markers =
    slow: tests that require ffmpeg and produce rendered video output (run with -m slow)
